import React from "react";
import { StyleSheet, View, Text } from "react-native";

export const Menu = () => {
  return (
    <View>
      <Text>menu</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
